def add_one(num):
    #I'm allowed to declare
    #local variables (variables
    #that only belong to this
    #function
    ans = num + 1
    return ans


print(add_one(5))
print(add_one(10))
print(add_one(99))
print(add_one("banana"))
