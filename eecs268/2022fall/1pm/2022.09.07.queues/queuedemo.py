#queue.py

from node import Node

class Queue:
    def __init__(self):
        self._front = None
        self._back = None

    def is_empty(self):
        #is the queue empty?

    def enqueue(self, entry):
        #place the entry in a node
        #add the node to the back

    def dequeue(self):
        #remove the front
