#stack.py

class Stack:
    def __init__(self):
        self._top = None

    def push(self, entry):
        #create a node and place the
        #entry inside of it
        #place it at the top

    def is_empty(self):
        #return True if stack is empty
        #or False otherwise

    def pop(self):
        #if the pop is possible, remove the top
        #and return the popped value
        #otherwise raise RuntimeError
