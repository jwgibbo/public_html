#linkedlist.py

from node import Node

class LinkedList:
    def __init__(self):
        self._front = None
        self._length = 0


    def get_entry(self, index):
        #define today (feb 24)!

    def insert(self, index, entry):
        #define today (feb 24)!

    def remove(self, index):
        #define today (Mardi Gras!)

    def clear(self):
        #empties the list
        #bonus board work (Mardi Gras)

    def set_entry(self, index, entry):
        #replace value at index with
        #entry
        #bonus board work (Mardi Gras)
