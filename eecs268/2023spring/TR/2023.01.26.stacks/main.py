#main.py
from node import Node

def main():
    first = Node("A")
    temp = Node("B")
    #Board work finish main to
    #create and assemble nodes in the
    #notes
    front.next = temp
    temp = Node("C")
    first.next.next = temp
    
