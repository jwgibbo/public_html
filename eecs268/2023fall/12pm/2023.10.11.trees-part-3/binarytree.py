#binarytree.py

from binarynode import BinaryNode

class BinaryTree:
    def __init__(self):
        self._root = None

    def add(self, entry):
        #for now, assume this works

    def search(self, target):
        return self._rec_search(target, self._root)

    def _rec_search(self, target, cur_node):
        #do the recursive search
        #first board of today: define _rec_search
        
    def count_nodes(self):
        return self._rec_count_nodes(self.root)

    def _rec_count_nodes(self, cur_node:
        #define next time
